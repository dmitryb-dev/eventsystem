#ifndef EVENT_SYSTEM_UTILS_H
#define EVENT_SYSTEM_UTILS_H

#define forInts(min, max, name) \
	for (int name = 0; name < max; name++)

#define repeat(times) \
	for (int _i = 0; _i < times; _i++)

#endif
